# Debug 记录 2026-06-02

> 本次主要工作:把 LR2 回放判定对齐到原版真值,并修正 beatoraja 判定窗口。
> 改动均在分支 `fix/judgement-matching`。

## TL;DR
- **LR2 判定现已与回放自带的 op210 真值逐档完全相等**(PGREAT/GREAT/GOOD/BAD/漏判/空POOR/总数 共 7 项全等)。
- 修了一个 **beatoraja 判定窗口 rank 缩放反了** 的 bug(按 beatoraja 源码校正)。
- 顺手修了 GUI Judge Config 里 BD 恒显示 0 的小 bug。

---

## 一、LR2 判定重写(`judgement_engine.cpp` / `lr2rep_parser.cpp`)

### 背景
原判定循环和 LR2 客户端对不上。用 `.lr2rep` 自带的 op210(LR2 逐音符判定真值)做对拍,差距很大。

### 根因 & 修复
依据 OpenLR2 `Scene04_Play.cpp ProcSinglenote` + 原版 `LR2body.exe` 反汇编:
1. **一次按键判了多个音符** → 改成一次按键最多判一个;只有 BAD 之后才顺延下一个音符(`Scene04_Play.cpp:852`)。
2. **空POOR 规则错** → 只在"音符在前方 200~1000ms"时算空POOR;>1000ms 的按键直接忽略;漏判在 press 超过音符 +200ms(BD)时记。
3. **op10(搓盘另一方向)不该判** → LR2 把它存进 `buttonInput[10]`,判定循环只读 `0..9`,所以忽略 op10。
4. **整数毫秒** → LR2 用整数毫秒的音符时间 + 整数 ms 的 press 时间;我们之前用全精度 double,边界音符会翻档(例:21.68ms 我们判 PG、LR2 截成 22ms 判 GR)。改成整数毫秒后边界对齐。
5. 判定窗口数值本来就对(EASY PG±21 等,和 `LR2body.exe @0x42BE50` 反汇编逐字节相同),**没动**。

### 验证(可复现)
```
bmv.exe testfiles\anata_g24.bme out.png --replay testfiles\eeff3a9a7cd557a05308ba1296752db2.lr2rep
```
控制台 op210 对照结果:

| 等级 | LR2(op210) | 重算 |
|---|---|---|
| PGREAT | 1159 | 1159 |
| GREAT | 885 | 885 |
| GOOD | 166 | 166 |
| BAD | 36 | 36 |
| 漏判 POOR | 6 | 6 |
| 空POOR | 32 | 32 |
| 总数 | 2284 | 2284 |

> - 位置对照里还会显示十几个"不一致",那是**同 tick 和弦音符的排序假象**(op210 没存音符 ID),逐档数量已完全相等。
> - 关于 offset:该回放整体偏快(均值约 −10ms),但 **PG 段均值约 −3ms,接近 0**,是玩家真实手感而非工具误差(另一份 beatoraja 回放 PG 段均值约 +1.5ms,佐证我们的时间校准是对的)。

---

## 二、beatoraja 判定窗口修正(`judge_profile.cpp`)

### 根因
原 beatoraja 窗口表的 **rank 缩放反了**:把 VERY_HARD 当成最宽(125%)、EASY 当成最严(50%)。按 `beatoraja-src/JudgeProperty.java`,实际应是 VERY_HARD 最严(25%)、EASY=100%。

### 修复
改成 vanilla SEVENKEYS:PG±20 / GR±60 / GD±150、非对称 BD(早220/晚280)、MS 固定(早500/晚150);`#RANK 0..3 → {25,50,75,100}%`。

### ⚠ 注意
beatoraja 的 `.brd` 回放**没有像 op210 那样的逐音符判定真值**,所以这块**无法像 LR2 那样自检验证**,只能按官方源码校正窗口数值。判定循环对 BD/MS 的非对称目前仍是近似(用对称 abs)。要做到完全忠实需要单独写 beatoraja 判定路径。

---

## 三、改了哪些文件
| 文件 | 改动 |
|---|---|
| `src/analysis/judgement_engine.cpp` | 判定循环重写、整数毫秒、分档诊断、逐音符对照 |
| `src/replay/lr2rep_parser.cpp` | op10 不参与判定 |
| `src/app/panels/chart_view.cpp` | 修 Judge Config 里 BD 恒为 0 的显示 bug |
| `src/judge/judge_profile.cpp` | beatoraja 窗口校正 |

---

## 四、后续(本次未做)
- beatoraja 判定路径的非对称微秒实现(无 oracle,需谨慎)。
- 回放解析模块(`src/replay/`)的解耦重构 —— 解析强耦合 `TimeMap`、两个 parser 事件模型不一致等,另有单独的重构方案稿。
